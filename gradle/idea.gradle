apply plugin: "idea"

idea {
  project {
    jdkName "1.8"
    languageLevel "JDK_1_8"
    vcs = "Git"
    ipr {
      withXml {
        def node = it.asNode()
        node.append(new XmlParser().parse(file("${rootDir}/gradle/idea/codeStyleSettings.xml")))
      }
    }
  }
}
