apply plugin: "io.ratpack.ratpack-java"

repositories {
  jcenter()
}

dependencies {
  compile "io.ratpack:ratpack-handlebars:1.0.0"
}

run {
  systemProperty "ratpack.reloadable", "true"
}

sourceCompatibility = "1.8"
targetCompatibility = "1.8"
